ProductsManager component created at src/components/ProductsManager.jsx.
Usage:
- Import and mount <ProductsManager /> in your app (e.g. App.jsx).
- Set REACT_APP_API_URL env var to backend URL (eg http://localhost:5000) or leave blank if using proxy.
